<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 860 220" role="img" aria-labelledby="title desc">
  <title id="title">GitHub metrics placeholder</title>
  <desc id="desc">Fallback asset shown until the metrics workflow can generate the real metrics.svg file.</desc>
  <rect width="860" height="220" rx="18" fill="#0D1117"/>
  <rect x="18" y="18" width="824" height="184" rx="14" fill="#161B22" stroke="#1F6FEB" stroke-width="2"/>
  <text x="48" y="82" fill="#F0F6FC" font-family="Arial, Helvetica, sans-serif" font-size="28" font-weight="700">
    GitHub metrics unavailable
  </text>
  <text x="48" y="122" fill="#C9D1D9" font-family="Arial, Helvetica, sans-serif" font-size="18">
    This repository needs the METRICS_TOKEN secret to generate the real metrics.svg asset.
  </text>
  <text x="48" y="156" fill="#58A6FF" font-family="Arial, Helvetica, sans-serif" font-size="18">
    Until then, the README shows this fallback instead of a broken image.
  </text>
</svg>
